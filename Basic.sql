SELECT * FROM 테이블 이름 WHERE author = 'egoing';
SELECT * FROM 테이블 이름 WHERE author = 'egoing' ORDER BY id DESC;
SELECT * FROM 테이블 이름 WHERE author = 'egoing' ORDER BY id ASC;

INSERT INTO 테이블 이름 (columns1, columns2, columns3) 
VALUES (columns1의 값, columns2의 값, columns3의 값);

SELECT * FROM 테이블 이름 groupby columns;